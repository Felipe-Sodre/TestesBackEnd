<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TestesBackEnd.Paginas.Propriedades"
             Title="Propriedades">
    <VerticalStackLayout>

        <Entry x:Name="Entry1" 
               Placeholder="Digite uma cor">
               </Entry>

        <Button x:Name="Botão1" 
                Clicked="Botão1_Clicked" 
                Text="Contar">
                </Button>

        
        

    </VerticalStackLayout>
</ContentPage>