<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TestesBackEnd.MainPage">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Aleatório" Clicked="ToolbarItem_Clicked_5"/>
        <ToolbarItem Text="Jogo" Clicked="ToolbarItem_Clicked_3"/>
        <ToolbarItem Text="Cores" Clicked="ToolbarItem_Clicked_6"/>
        <ToolbarItem Text="Internet" Clicked="ToolbarItem_Clicked_2"/>
        <ToolbarItem Text="Calculadora" Clicked="ToolbarItem_Clicked"/>
        <ToolbarItem Text="Notificações" Clicked="ToolbarItem_Clicked_1"/>
        <ToolbarItem Text="Informações" Clicked="ToolbarItem_Clicked_4"/>
        <ToolbarItem Text="SQLite" Clicked="SQLite"/>
        <ToolbarItem Text="SQL" Clicked="SQL"/>
        <ToolbarItem Text="Midia" Clicked="ToolbarItem_Clicked_7"/>
        <ToolbarItem Text="Propriedades" Clicked="ToolbarItem_Clicked_8"/>
        <ToolbarItem Text="Login" Clicked="ToolbarItem_Clicked_9"/>
    </ContentPage.ToolbarItems>

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Image
                Source="back.png"
                HeightRequest="185"
                Aspect="AspectFit"
                SemanticProperties.Description="dot net bot in a race car number eight" />

            <Label
                Text="Sodré Tec"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Label
                Text="Aplicativo de testes"
                Style="{StaticResource SubHeadline}"
                SemanticProperties.HeadingLevel="Level2"
                SemanticProperties.Description="Welcome to dot net Multi platform App U I" />

            <Button
                x:Name="CounterBtn"
                Text="Click me" 
                SemanticProperties.Hint="Counts the number of times you click"
                Clicked="OnCounterClicked"
                HorizontalOptions="Fill" />
        </VerticalStackLayout>
    </ScrollView>
  

</ContentPage>
